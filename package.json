{
  "name": "gouv-widgets",
  "version": "0.1.0",
  "description": "Bibliothèque de Web Components de dataviz pour sites gouvernementaux",
  "type": "module",
  "main": "dist/gouv-widgets.umd.js",
  "module": "dist/gouv-widgets.esm.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:shared": "npm run build --workspace=@gouv-widgets/shared",
    "build:apps": "npm run build --workspace=@gouv-widgets/app-favorites && npm run build --workspace=@gouv-widgets/app-playground && npm run build --workspace=@gouv-widgets/app-sources && npm run build --workspace=@gouv-widgets/app-builder-ia && npm run build --workspace=@gouv-widgets/app-builder",
    "build:all": "npm run build:shared && npm run build && npm run build:apps",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "npm run build:app && tauri build",
    "build:app": "node scripts/build-app.js"
  },
  "keywords": [
    "web-components",
    "dsfr",
    "dataviz",
    "gouvernement",
    "lit"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@gouvfr/dsfr-chart": "^2.0.4",
    "lit": "^3.1.0"
  },
  "devDependencies": {
    "@open-wc/testing": "^4.0.0",
    "@tauri-apps/cli": "^2.10.0",
    "@vitest/coverage-v8": "^1.6.1",
    "jsdom": "^28.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vitest": "^1.2.0"
  }
}
