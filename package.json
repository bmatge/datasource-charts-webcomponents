{
  "name": "gouv-widgets",
  "version": "0.2.3",
  "description": "Bibliothèque de Web Components de dataviz pour sites gouvernementaux",
  "type": "module",
  "main": "dist/gouv-widgets.umd.js",
  "module": "dist/gouv-widgets.esm.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && vite-node scripts/build-skills-json.ts",
    "build:shared": "npm run build --workspace=@gouv-widgets/shared",
    "build:apps": "npm run build --workspace=@gouv-widgets/app-favorites && npm run build --workspace=@gouv-widgets/app-playground && npm run build --workspace=@gouv-widgets/app-sources && npm run build --workspace=@gouv-widgets/app-builder-ia && npm run build --workspace=@gouv-widgets/app-builder && npm run build --workspace=@gouv-widgets/app-dashboard && npm run build --workspace=@gouv-widgets/app-monitoring",
    "build:all": "npm run build:shared && npm run build && npm run build:apps",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "npm run build:all && npm run build:app && tauri build",
    "build:app": "node scripts/build-app.js",
    "test:e2e": "playwright test"
  },
  "keywords": [
    "web-components",
    "dsfr",
    "dataviz",
    "gouvernement",
    "lit"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@gouvfr/dsfr-chart": "^2.0.4",
    "lit": "^3.1.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.1",
    "@open-wc/testing": "^4.0.0",
    "@playwright/test": "^1.58.2",
    "@tauri-apps/cli": "^2.10.0",
    "@vitest/coverage-v8": "^3.0.0",
    "jsdom": "^28.0.0",
    "typescript": "^5.3.0",
    "vite": "^6.4.1",
    "vitest": "^3.0.0"
  }
}
